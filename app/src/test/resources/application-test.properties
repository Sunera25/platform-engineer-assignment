spring.datasource.url=jdbc:postgresql://${TEST_DB_HOST:localhost}:${TEST_DB_PORT:5432}/${TEST_DB_NAME:taskflow}
spring.datasource.username=${TEST_DB_USER:taskflow_admin}
spring.datasource.password=${TEST_DB_PASSWORD:password}
spring.jpa.hibernate.ddl-auto=create-drop
logging.level.org.springframework=WARN
logging.level.com.taskflow=WARN
